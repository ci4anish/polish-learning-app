// Copy this file to Config.xcconfig and fill in your values.
// Config.xcconfig is gitignored — never commit it.
//
// NOTE: Don't use // in values (xcconfig treats it as a comment).

API_BASE_URL = http:$()/$()/localhost:8787
